package routes

// func HandleAdminRoutes(api *mux.Router, logger *zap.Logger) {

// 	authHandler := handlers.NewAdminHandler(logger)

// 	api.HandleFunc("/users", authHandler.RegisterUser).Methods(http.MethodPost)
// 	api.HandleFunc("/notes", authHandler.LoginUser).Methods(http.MethodPost)
// }
